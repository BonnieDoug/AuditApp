{
  "name": "auditapi-frontend",
  "version": "1.0.0",
  "description": "Audit API Frontend, this should be as seperate as possible from the backend, simply relying on a JSON string sent from the backend to create views. There should be no reason why this front end couldnt be converted to Ruby/JS/VB or what ever, simply by changing the forloops...",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Doug",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^2.2.0"
  }
}
